---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: garbidz-staging
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: ghcr.io/adamzv/garbidz
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  # generators:
  # - git:
  #     repoURL: https://github.com/opspresso/argocd-env-demo.git
  #     revision: HEAD
  #     files:
  #       - path: envs/staging/garbidz/env.yaml
  project: default
  source:
    repoURL: https://github.com/adamzv/garbidz-gitops.git
    targetRevision: main
    path: garbidz
    helm:
      valueFiles:
      - ../envs/staging/garbidz/values.yaml
      parameters:
        - name: "replicaCount"
          value: "1"
        - name: "image.tag"
          value: pr-55
  destination:
    server: https://kubernetes.default.svc
    namespace: garbidz-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - Validate=true
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true